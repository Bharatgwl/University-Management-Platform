// utils/auth.js (optional if you want to keep it separate)
export function isAuth() {
  // You can customize this logic. Example using localStorage token:
//   return !!localStorage.getItem("token");

  return true;
}
